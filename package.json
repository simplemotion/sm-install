{
  "name": "sm-install-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "install.simplemotion.com — static welcome/install site; TS bundled by esbuild at deploy.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node sm-build.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.2"
  }
}
